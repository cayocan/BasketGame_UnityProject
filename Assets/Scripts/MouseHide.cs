using UnityEngine;
using System.Collections;

public class MouseHide : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Cursor.visible = false;//Faz com que o mouse desapareça da tela.
	}
	
	// Update is called once per frame
	void Update () {
        
	}
}
